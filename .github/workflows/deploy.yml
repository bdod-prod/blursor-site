name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
      - 'auto/**'
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  deployments: write
  pull-requests: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Determine branch name
        id: branch
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "branch=${{ github.head_ref }}" >> "$GITHUB_OUTPUT"
          else
            BR="${GITHUB_REF#refs/heads/}"
            echo "branch=$BR" >> "$GITHUB_OUTPUT"
          fi

      - name: Run tests
        run: npm test

      - name: Regenerate research/index.html from article metadata
        run: node scripts/build-research-index.js

      - name: Build validated public bundle
        run: npm run build:pages

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4.80.0"
          command: pages deploy .pages-public --project-name=blursor --branch=${{ steps.branch.outputs.branch }} --commit-dirty=true
